NODE_ENV=development
PORT=5000

# MongoDB Atlas. If the password has special characters, URL-encode them (@ becomes %40).
# Local alternative: mongodb://127.0.0.1:27017/upang_delivers
MONGODB_URI=mongodb+srv://upang_app_user:UpangDelivers2026@cluster0.otqaswf.mongodb.net/upang_delivers?retryWrites=true&w=majority

# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=0f4e503d19dccad567d2af081b790d392505564fa699965eacec4342b6145cd9
JWT_EXPIRES_IN=2h

BCRYPT_ROUNDS=12

# Rate limiting on /api/auth/*
AUTH_RATE_WINDOW_MS=60000
AUTH_RATE_MAX=5

# CORS: your frontend's origin (use the deployed URL in production)
CORS_ORIGIN=http://localhost:5173
